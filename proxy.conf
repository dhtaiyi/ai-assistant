# 全局代理配置
# 所有外部HTTP/HTTPS请求都通过代理

http_proxy=http://localhost:13128
https_proxy=http://localhost:13128
HTTP_PROXY=http://localhost:13128
HTTPS_PROXY=http://localhost:13128

# 不使用代理的地址（本地）
no_proxy=localhost,127.0.0.1,.local
