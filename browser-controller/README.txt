# 浏览器控制程序

## 一、使用方法

### 1️⃣ 第一次使用（安装依赖）

**双击 `install.bat`**

- 自动检查Python
- 自动安装playwright
- 自动安装Chromium浏览器

### 2️⃣ 运行程序

**双击 `run.bat`**

或在命令行运行：
```bash
run -u https://www.10jqka.com.cn
```

## 二、常用命令

| 功能 | 命令 |
|------|------|
| 打开同花顺 | `quick ths` |
| 打开百度 | `quick baidu` |
| 获取股票数据 | `quick stock` |
| 打开网页 | `run -u https://...` |
| 获取股票 | `run -u https://... --stock` |
| 点击元素 | `run -u https://... -c .btn` |
| 滚动页面 | `run -u https://... -s down` |

## 三、桌面快捷方式

双击 `create-shortcuts.bat` 创建桌面快捷方式：
- 📊 打开同花顺 - 一键获取股票数据

## 四、示例

```bash
# 打开同花顺并获取股票数据
run -u https://www.10jqka.com.cn --stock

# 打开东方财富
run -u https://quote.eastmoney.com --stock

# 滚动到页面底部
run -u https://www.10jqka.com.cn -s bottom
```

## 五、问题排查

| 问题 | 解决方法 |
|------|----------|
| Python未安装 | 运行 `install.bat` 会提示 |
| playwright安装失败 | 手动运行: `pip install playwright` |
| Chromium安装失败 | 手动运行: `python -m playwright install chromium` |

## 六、文件说明

```
browser-controller/
├── install.bat          # 一键安装（首次使用）
├── run.bat             # 运行程序
├── quick.bat           # 常用快捷命令
├── create-shortcuts.bat # 创建桌面快捷方式
├── main.py             # 命令行入口
├── browser.py          # 核心程序
└── README.md           # 说明文档
```
